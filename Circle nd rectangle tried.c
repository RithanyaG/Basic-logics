

#include <stdio.h>

 main()
#define PI=3.14
{
    int length,breadth,radius,area_r,perimeter,area_c,circum;
    
    scanf("%d%d%d",&length,&perimeter,&radius);
    
    area_r=length*breadth;
    perimeter=2*length*breadth;
    area_c=pi*r*r;
    circum=2*pi*r;
    
    
    printf("area_r=%d",area_r);
    printf("perimeter=%d",perimeter);
    printf("area_c=%d",area_c);
    printf("circum=%d",circum);

    
}
